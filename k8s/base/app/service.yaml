apiVersion: v1
kind: Service
metadata:
  name: springboot-cicd-eks-svc
  namespace: team-app
spec:
  type: LoadBalancer   # <-- external access
  selector:
    app: springboot-cicd-eks
  ports:
    - name: http
      port: 80         # exposed on LB
      targetPort: 8080 # app inside pod

